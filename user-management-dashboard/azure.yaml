# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: user-management-dashboard
metadata:
  template: user-management-dashboard

services:
  web:
    project: .
    language: js
    host: staticwebapp
